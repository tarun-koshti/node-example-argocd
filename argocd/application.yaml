apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: node-example
  namespace: argocd
  annotations:
    argocd-image-updater.argoproj.io/image-list: node=tarunkoshti/node-example-argocd
    argocd-image-updater.argoproj.io/node.update-strategy: latest
spec:
  project: default

  source:
    repoURL: https://github.com/tarun-koshti/node-example-argocd.git
    targetRevision: main
    path: k8s

  destination:
    server: https://kubernetes.default.svc
    namespace: node-example

  syncPolicy:
    automated:
      prune: true       # delete resources removed from Git
      selfHeal: true    # fix manual changes in cluster
    syncOptions:
      - CreateNamespace=true
